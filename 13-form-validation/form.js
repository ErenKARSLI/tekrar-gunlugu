const form  = document.getElementById("kullaniciFormu");
const hata  = document.getElementById("hataMesaji");

form.addEventListener("submit", e => {
  e.preventDefault();
  // alanları oku ve validate et...
});
